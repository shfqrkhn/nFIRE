{
  "name": "nfire",
  "version": "10.1.40",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node tests/static-regression.mjs",
    "test:artifact": "bash scripts/verify_artifact_consistency.sh",
    "test:ui": "playwright test tests/ui-smoke.spec.mjs",
    "qa": "npm test && npm run test:artifact && npm run test:ui"
  },
  "devDependencies": {
    "@playwright/test": "1.61.1"
  }
}
